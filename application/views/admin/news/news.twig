{% extends 'layouts/admin.twig' %}

{% block title %}Новости админки{% endblock %}


	{% block content %}
		
        <div class="col-md-10 content">
            <div class="panel panel-default">
                <div class="panel-heading">
                    Статьи
                </div>
                <div class="panel-body">
                    <div class="col-lg-12"> 
                    <div class="table-responsive"> 
                    <table class="table table-bordered table-hover table-striped"> 
                    <thead> 
                    <tr> 
                    <th>№</th> 
                    <th>Заголовок</th> 
                    <th>описание</th> 
                    <th>дата публикации</th>  
                    <th></th>  
                    </tr> 
                    </thead> 
                    <tbody> 
                    {% for item in article %} 
                    <tr> 
                    <td>{{ item.id }}</td>    
                    <td>{{ item.title }}</td>
                    <td>{{ item.discription }}</td>
                    <td>{{ item.date_time }}</td>
                     
                    <td> <a href=""><button class="btn btn-success" >Добавить</button><button class="btn btn-primary" >Редактировать</button><button class="btn btn-danger" >удалить</button></a></td> 
                    </tr> 
                    {% endfor %}

                    </tbody> 
                    </table> 
                    </div> 
                    </div> 
                </div>
            </div>
        </div>

	{% endblock %} 